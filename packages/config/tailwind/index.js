// Tailwind v4 uses CSS-based configuration
// This file is for tooling that still expects JS config
module.exports = {
  // Minimal config for tools that need it
  content: [],
  // Most configuration is now in CSS files
};
